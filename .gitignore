.DS_Store
*.log
helper/loudini-helper
menubar/Loudini.app/
menubar/AppIcon.iconset/
# release build output (Loudini.dmg / Loudini.zip)
dist/

# pnpm project — npm lockfiles must never be committed
package-lock.json
yarn.lock
node_modules/

# Claude Code writes a machine-local lock here; the tracked skill stays tracked.
.claude/scheduled_tasks.lock
